export enum AssertTypes {
  None = 'none',
  Equal = 'equal',
  NotEqual = 'notEqual',
  Contains = 'contains',
  DoesNotMatch = 'doesNotMatch',
  EndsWith = 'endsWith',
  Matches = 'matched',
  StartsWith = 'startsWith'
}
